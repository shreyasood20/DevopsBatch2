pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Building..."
                bat 'echo Build step running on Windows'
            }
        }
        stage('Test') {
            steps {
                echo "Testing..."
                bat 'echo Test step running on Windows'
            }
        }
    }
}
